{% extends "base.html.twig" %}

{% block title %}{{ error }} - {{ message }}{% endblock %}

{% block body %}
    <div class="jumbotron-fluid">
        <div class="container">
            <h3>{{ error }} - {{ message }}</h3>
            <a href="{{path('app_stream')}}">
            <button class="btn btn-secondary">Back to posts</button>
        </div>
    </div>
{% endblock %}
