{% extends 'base.html.twig' %}
{% block title %}{% endblock %}
{% block body %}
    <section class="other-section"> 
        <h3>UPLOAD</h3>
        <div class="container">
                {{ form_start(form)}}
                {{ form_row(form.mediaFile, { 'label': 'Artwork' }) }}
                {{ form_widget(form) }}
                <div id="btn-form-edit">
                    <a class="big-btn" href="{{ path('app_stream')}}">BACK</a>
                    <input class="big-btn" type="submit" value="UPDATE">
                <div>
                {{ include('stream/delete.html.twig') }}
                {{ form_end (form) }}  
        </div>
    </section>
{% endblock %}
