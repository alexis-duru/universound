<header>
        <div id="header-top">
            <div id="header-elements">
                <div id="logo">
                    <img src="{{asset('images/logo/universound.jpg')}}" id="typo-universound"></img>
                    <img src="{{asset('images/logo/logo.jpg')}}" id="logo-universound">
                </div>
                    
                <div class="social-media">
                    <a href="http://facebook.com"><i class="fab fa-facebook-f"></i></a>
                    <a href="http://twitter.com"><i class="fab fa-twitter"></i></a>
                    <a href="http://instagram.com"><i class="fab fa-instagram"></i></a>
                </div>
            </div>
        </div>
            {% if not app.user %}
            <nav>
                <ul>
                    <li>
                        <a href="{{path('app_homepage')}}">HOME</a>
                    </li>
                    <li>
                        <a href="{{path('app_stream')}}">STREAM</a>
                    </li>
                    <li>
                        <a href="{{path('app_upload')}}">UPLOAD</a>
                    </li>
                    <span></span>
                    <li>
                        <a href="{{path('app_login')}}">LOGIN</a><a href="{{path('app_register')}}">REGISTER</a>
                    </li>
                </ul>
            </nav>
            {% else %}
            <nav>
                <ul>
                    <li>
                        <a href="{{path('app_homepage')}}">HOME</a>
                    </li>
                    <li>
                        <a href="{{path('app_stream')}}">STREAM</a>
                    </li>
                    <li>
                        <a href="{{path('app_upload')}}">UPLOAD</a>
                    </li>
                    <li>
                        <a href="{{path('app_profil')}}"><span id="id-user">{{ app.user.username }}</span></a><a href="{{path('app_logout')}}">DISCONNECT</a>
                    </li>
                </ul>
            </nav>
            {% endif %}
    </header>
